import { Eye, Wrench } from 'lucide-react';
import { useNavigate } from 'react-router';
import StatusBadge from '../../components/common/StatusBadge';
import PriorityBadge from '../../components/common/PriorityBadge';
import { EmptyBlock } from '../../components/common/Feedback';

const MaintenanceTable=({requests=[],basePath,action})=>{const nav=useNavigate();if(!requests.length)return <EmptyBlock title="No maintenance requests" text="New issues will appear here as soon as they are reported."/>;return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[950px]"><thead className="bg-slate-50"><tr>{['Request','Asset','Issue','Reported By','Priority','Status','Action'].map(h=><th key={h} className="px-4 py-3 text-left text-[9px] font-bold uppercase tracking-wider text-slate-400">{h}</th>)}</tr></thead><tbody>{requests.map(r=><tr key={r.requestId} className="border-t border-slate-100 hover:bg-blue-50/20"><td className="px-4 py-4 text-xs font-bold text-blue-600">{r.requestId}</td><td className="px-4 py-4"><p className="text-xs font-semibold text-slate-700">{r.assetId?.assetName||r.assetId?.assetName||'—'}</p><p className="text-[9px] text-slate-400">{r.assetId?.assetId||'—'}</p></td><td className="max-w-[230px] px-4 py-4 text-[10px] text-slate-600"><span className="line-clamp-2">{r.issueDescription}</span></td><td className="px-4 py-4 text-[10px] text-slate-600">{r.reportedBy?.name||'—'}</td><td className="px-4 py-4"><PriorityBadge priority={r.priority}/></td><td className="px-4 py-4"><StatusBadge status={r.status}/></td><td className="px-4 py-4"><div className="flex gap-2"><button onClick={()=>nav(`${basePath}/${r.requestId}`)} className="inline-flex h-8 w-8 items-center justify-center rounded-lg bg-blue-50 text-blue-600 hover:bg-blue-100"><Eye size={14}/></button>{action?.(r)}</div></td></tr>)}</tbody></table></div></div>};export default MaintenanceTable;

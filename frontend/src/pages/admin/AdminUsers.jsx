import { useEffect,useState } from 'react';
import { Plus, Search, ShieldCheck, Trash2 } from 'lucide-react';
import PageHeader from '../../components/layout/PageHeader';
import SectionCard from '../../components/common/SectionCard';
import StatusBadge from '../../components/common/StatusBadge';
import Modal from '../../components/common/Modal';
import Button from '../../components/common/Button';
import FormField,{inputClass,selectClass} from '../../components/common/FormField';
import {Alert,LoadingBlock} from '../../components/common/Feedback';
import {userService} from '../../services/dataService';
const blank={name:'',email:'',password:'',role:'STAFF',phone:'',designation:'',department:'',specialization:''};
const AdminUsers=()=>{const[data,setData]=useState([]);const[q,setQ]=useState('');const[open,setOpen]=useState(false);const[form,setForm]=useState(blank);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState('');const load=async()=>{try{setLoading(true);setData((await userService.list({page:1,limit:100,search:q}))?.users||[])}catch(e){setError(e.response?.data?.message||'Unable to load users.')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const create=async e=>{e.preventDefault();try{setSaving(true);await userService.create(form);setForm(blank);setOpen(false);await load()}catch(e){setError(e.response?.data?.message||'Unable to create user.')}finally{setSaving(false)}};const remove=async id=>{if(!confirm(`Delete ${id}?`))return;try{await userService.remove(id);await load()}catch(e){setError(e.response?.data?.message||'Unable to remove user.')}};const rows=data.filter(u=>[u.name,u.email,u.userId,u.role,u.department].some(v=>String(v||'').toLowerCase().includes(q.toLowerCase())));if(loading)return <LoadingBlock text="Loading authorized users..."/>;return <div className="animate-fade-in"><PageHeader title="User Management" description="Create authorized hospital accounts. New users are stored in MongoDB and can sign in immediately with their assigned credentials." icon={ShieldCheck} action={<Button icon={Plus} onClick={()=>setOpen(true)}>New User</Button>}/>{error&&<div className="mb-4"><Alert>{error}</Alert></div>}<div className="mb-5 relative"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search user, role, department or email..." className="h-11 w-full rounded-xl border border-slate-200 bg-white pl-10 pr-4 text-xs shadow-sm outline-none focus:border-blue-500"/></div><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[900px]"><thead className="bg-slate-50"><tr>{['User','Role','Department','Contact','Status','Action'].map(h=><th key={h} className="px-4 py-3 text-left text-[9px] font-bold uppercase tracking-wider text-slate-400">{h}</th>)}</tr></thead><tbody>{rows.map(u=><tr key={u.userId} className="border-t border-slate-100 hover:bg-blue-50/20"><td className="px-4 py-4"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-blue-50 text-[10px] font-bold text-blue-600">{u.name?.split(' ').map(x=>x[0]).slice(0,2).join('')}</div><div><p className="text-xs font-bold text-[#09284d]">{u.name}</p><p className="text-[9px] text-blue-600">{u.userId}</p><p className="text-[9px] text-slate-400">{u.email}</p></div></div></td><td className="px-4 py-4"><StatusBadge status={u.role==='ADMIN'?'ACTIVE':u.role}/><p className="mt-1 text-[9px] text-slate-400">{u.designation}</p></td><td className="px-4 py-4 text-[10px] text-slate-600">{u.department||'—'}</td><td className="px-4 py-4 text-[10px] text-slate-600">{u.phone||'—'}</td><td className="px-4 py-4"><StatusBadge status={u.accountStatus}/></td><td className="px-4 py-4"><button onClick={()=>remove(u.userId)} className="inline-flex h-8 w-8 items-center justify-center rounded-lg text-red-400 hover:bg-red-50 hover:text-red-600"><Trash2 size={14}/></button></td></tr>)}</tbody></table></div></div><Modal isOpen={open} onClose={()=>setOpen(false)} title="Create Authorized User" description="The password is hashed by the backend before it is stored in MongoDB." size="lg"><form onSubmit={create} className="grid gap-4 sm:grid-cols-2"><FormField label="Full name" required><input required className={inputClass} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></FormField><FormField label="Email" required><input required type="email" className={inputClass} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></FormField><FormField label="Password" required><input required minLength={6} type="password" className={inputClass} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></FormField><FormField label="Role" required><select className={selectClass} value={form.role} onChange={e=>setForm({...form,role:e.target.value})}>{['STAFF','TECHNICIAN','ADMIN'].map(x=><option key={x}>{x}</option>)}</select></FormField><FormField label="Phone"><input className={inputClass} value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></FormField><FormField label="Designation"><input className={inputClass} value={form.designation} onChange={e=>setForm({...form,designation:e.target.value})}/></FormField><FormField label="Department"><input className={inputClass} value={form.department} onChange={e=>setForm({...form,department:e.target.value})}/></FormField><FormField label="Specialization"><input className={inputClass} value={form.specialization} onChange={e=>setForm({...form,specialization:e.target.value})}/></FormField><div className="sm:col-span-2 flex justify-end gap-2"><Button variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit" loading={saving}>Create User</Button></div></form></Modal></div>};export default AdminUsers;

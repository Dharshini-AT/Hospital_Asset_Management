import { useEffect,useState } from 'react';
import { Search, Users, Wrench } from 'lucide-react';
import PageHeader from '../../components/layout/PageHeader';
import SectionCard from '../../components/common/SectionCard';
import StatusBadge from '../../components/common/StatusBadge';
import {Alert,LoadingBlock} from '../../components/common/Feedback';
import {userService} from '../../services/dataService';
const AdminTechnicians=()=>{const[t,setT]=useState([]);const[q,setQ]=useState('');const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{userService.technicians().then(x=>setT(x||[])).catch(e=>setError(e.response?.data?.message||'Unable to load technicians.')).finally(()=>setLoading(false))},[]);const list=t.filter(x=>[x.name,x.userId,x.specialization,x.department].some(v=>String(v||'').toLowerCase().includes(q.toLowerCase())));if(loading)return <LoadingBlock text="Loading technician directory..."/>;return <div className="animate-fade-in"><PageHeader title="Technician Directory" description="Monitor availability, specialization and active workload before assigning maintenance." icon={Wrench}/>{error&&<Alert>{error}</Alert>}<div className="mb-5 mt-5 relative"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search technician, ID or specialization..." className="h-11 w-full rounded-xl border border-slate-200 bg-white pl-10 pr-4 text-xs shadow-sm outline-none focus:border-blue-500"/></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{list.map(x=><SectionCard key={x.userId} className="hover:-translate-y-1 transition-transform"><div className="flex items-start gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-blue-50 text-blue-600"><Users size={19}/></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-[#09284d]">{x.name}</p><p className="text-[9px] font-bold text-blue-600">{x.userId}</p><p className="mt-1 text-[10px] text-slate-400">{x.specialization||'General'} • {x.department||'—'}</p></div><StatusBadge status={x.availabilityStatus}/></div><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-50 p-3"><p className="text-[9px] text-slate-400">Active workload</p><p className="mt-1 text-lg font-bold text-[#09284d]">{x.activeWorkload||0}</p></div><div className="rounded-xl bg-slate-50 p-3"><p className="text-[9px] text-slate-400">Contact</p><p className="mt-1 truncate text-[10px] font-semibold text-slate-700">{x.phone||'Not added'}</p></div></div></SectionCard>)}</div></div>};export default AdminTechnicians;
